<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fallenangels">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FallenAngels">
        <activity
            android:name=".admin_pages.MainActivityAdmin"
            android:exported="false" />
        <activity
            android:name=".startup.Login"
            android:exported="false" />
        <activity
            android:name=".startup.Register"
            android:exported="false" />
        <activity
            android:name=".user_pages.MainActivity"
            android:exported="false" />
        <activity
            android:name=".startup.GetStarted"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>